name: endereco
queries:
  - insert: select * from public.endereco_insert_v1(:cep, :complemento, :numero, :rua, :bairro, :cidade, :estado);
  - update: select * from public.endereco_update_v1(:cep, :complemento, :numero, :rua, :bairro, :cidade, :estado, :id);
  - selectByEndereco: select * from public.endereco_select_by_endereco_v1(:cep, :complemento, :numero, :rua, :bairro, :cidade, :estado);
